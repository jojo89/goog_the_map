<div class="banner">
  <h1>{{ "Tweet Locator" }}</h1>
</div> 
<div class="display">
 <div id ="clear-tweet"><h3>Clear<br>Map</h3></div>
 <div id="map-canvas"></div>
   <div id="profile">
     <img class="profile-pic" ng-src="{{list.tweets[0].image}}" ng-show="{{1}}"/>
     <a id= "tweeter-page" href="https://twitter.com/crazyboutelvish/"><h3 class="screenname">twitter_username</h3></a>
     <div class="tweet"><p>The Tweet itself. #getit?</p></div>
   </div>
  <div id="list">
    <h1>Hottest Tweets</h1>
    <ol>
      <li ng-repeat="tweet in list.tweets" ng-show="tweet.canShow" >{{tweet.name}}- {{tweet.tweet}} + {{tweet.price | currency}}</li>
    </ol>
  </div>
  <div id="expose"> 
    <form class="round" id="srch_map" >
      <label for="zip">Where would you like to search!</label><input type="text" id="zip" name="zip" placeholder="Zip Code">
      <input id="zip_srch" class="button" type="button"  value="relocate" >
      <label for="phrase">Tweet Search</label><input type="text" id="phrase" name="phrase" placeholder="What you are looking for?">
      <input id="find_tweets" class="button" type="button"  value="Search!" >
    </form>
  </div>
  <div id="directions" ng-controller="ItemController as item"> 
    <h2>Instructions</h2>
    <p ng-class="{ highlight: item.isSet(1) }" ng-click="item.setItem(1)"> 1.To "relocate" the map window type the area code or name of the place where you would like to search for tweets in the first text box and click the relocate button. </p>
    <p ng-class="{ highlight: item.isSet(2) }" ng-click="item.setItem(2)">2. Enter the word or words by which you would like to filter. (when entering multiple words twitter will search only for tweets containing all of the words submitted.)</p>
    <p ng-class="{ highlight: item.isSet(3) }" ng-click="item.setItem(3)">3. click the "Search!" button to find your tweets!</p>
   </div>
 </div>
 <div ng-controller="FormController as form">
   <ul>
     <li ng-repeat="review in form.reviews">
       {{review.stars}}
       {{review.person}}
       {{review.text}}
       {{review.createdAt | date}}
     </li>
   </ul>
   <form name="reviewForm" ng-submit="reviewForm.$valid && form.addReview(review)" novalidate>
     <blockquote>
       <strong>{{form.object.stars}} Stars</strong>
       {{form.object.text}}
       <cite class="clearfix">—{{form.object.person}}</cite>
     </blockquote>
     <fieldset>
       <select ng-model="form.object.stars" ng-options="stars for stars in [5,4,3,2,1]" title="Stars" required>
         <option value="">Rate the Product</option>
       </select>
     </fieldset>
     <fieldset>
       <input ng-model="form.object.text" />
     </fieldset>
     <fieldset>
       <input ng-model="form.object.person" required/>
     </fieldset>
     <fieldset>
       <input type="submit">
     </fieldset>
   </form>
 </div>
   <div id="foot">
     
     <ul class="social">
       <li><a href="https://soundcloud.com/freezelapd"><img src="images/Soundcloud-white-round-64.png"></a></li>
       <li><h3 class="jojo"><a>a jojo89 joint</a></h3></li>
       <li><a href="https://github.com/jojo89"><img src="images/Github-64.png"></a></li>
     </ul> 
   </div>

